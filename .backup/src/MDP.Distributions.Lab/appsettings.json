{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.*": "Warning",
      "System.*": "Warning"
    }
  },

  "MDP.Distributions": {

    // DistributeContext
    "DistributeContext": {
      "ResourceLockRepository": "MemoryResourceLockRepository"
    },

    // ResourceLockRepository
    "MemoryResourceLockRepository": {}
  }
}