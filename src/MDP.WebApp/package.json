{
  "version": "1.0.0",
  "name": "mdp.webapp",
  "private": true,
  "devDependencies": {
    "gulp": "^4.0.2",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "bootstrap": "^5.1.0"
  },
  "scripts": {
    "publish": "npm install && gulp publish"
  }
}
