{
  "devDependencies": {
    "grunt": "^1.6.1",
    "gulp": "^4.0.2",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "admin-lte": "^3.1.0"
  },
  "scripts": {
    "publish": "npm install && gulp publish"
  }
}
